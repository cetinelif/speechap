<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.speechap"
    android:versionCode="1"
    android:versionName="1.0" >
    
    <uses-permission android:name="android.permission.SEND_SMS"></uses-permission>
      <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        
        android:theme="@android:style/Theme.Black">
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.speechap.MainActivity"
            android:label="@string/app_name" >
            
            
           
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="AramaYap" android:label="@string/app_name">
</activity>
 		<activity android:name="ContactBean" android:label="@string/app_name">
 		</activity>
         
      <activity android:name="ContactListActivity" android:label="@string/app_name">
 		</activity>
 		<activity android:name="ContactAdapter" android:label="@string/app_name">
 		</activity>
 		<activity android:name="SecilenKisi" android:label="@string/app_name">
 		</activity>
 		<activity android:name="Splash" android:label="@string/app_name">
 		      <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            
        </activity>
 		
         
    </application>

</manifest>
